// import { createApp } from 'vue'

import App from './App.vue'

import './index.css'

// es6模块，会自动发送请求，查找响应文件

// 根据你需要的文件，进行改写


// 1、默认会请求 main.js => 在后端将 main.js 中的内容进行改写操作，所有不带 ./ ../ / 的全部增加一个 /modules

// createApp(App).mount('#app')
